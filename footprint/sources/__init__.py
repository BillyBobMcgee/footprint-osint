"""Data sources queried by footprint.

Each source is defensive and relies only on public APIs. No source ever
returns plaintext credentials for a third party. They report *exposure*,
which is what remediation-focused OSINT work actually needs.
"""
